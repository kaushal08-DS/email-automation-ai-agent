'use client'; import {useEffect} from 'react'; import {api} from '../../../lib/api'; import {useRouter} from 'next/navigation'; export default function Callback(){const r=useRouter(); useEffect(()=>{api('/api/me').then(m=>r.replace(m.style_ready?(m.subscription.active?'/dashboard':'/subscription'):'/onboarding')).catch(()=>r.replace('/login'))},[]); return <div className="layout-center"><div className="login">Connecting your Gmail account…</div></div>}
