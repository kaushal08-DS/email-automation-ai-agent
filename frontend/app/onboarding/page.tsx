'use client'; import {useState} from 'react'; import {api} from '../../lib/api'; import {useRouter} from 'next/navigation'; export default function Onboarding(){const [text,setText]=useState('');const [busy,setBusy]=useState(false);const r=useRouter(); async function save(){setBusy(true);try{await api('/api/style/learn',{method:'POST',body:JSON.stringify({text})});r.push('/dashboard')}catch(e:any){alert(e.message)}finally{setBusy(false)}} return <div className="layout-center"><div className="login" style={{textAlign:'left'}}><h1>Let MailPilot learn your style</h1><p className="muted">Write a natural reply to this example. We use it to make future drafts sound like you.</p><div className="card"><b>Topic</b><p>Your client has asked to reschedule tomorrow’s meeting.</p></div><textarea className="textarea" placeholder="Write your reply…" value={text} onChange={e=>setText(e.target.value)}/><button className="btn primary" style={{marginTop:12}} onClick={save} disabled={busy}>{busy?'Learning…':'Learn my writing style'}</button></div></div>}
