'use client'; import {api} from '../../lib/api'; import Layout from '../../components/Layout'; export default function Settings(){return <Layout><h1>Settings</h1><div className="card"><h2>Account</h2><p className="muted">Sign out of MailPilot AI on this browser.</p><button className="btn danger" onClick={()=>api('/auth/logout',{method:'POST'}).then(()=>location.href='/login')}>Sign out</button></div></Layout>}
