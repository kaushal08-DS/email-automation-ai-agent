'use client'; import {useEffect,useState} from 'react'; import Layout from '../../components/Layout'; import {api} from '../../lib/api'; export default function Style(){const [profile,setProfile]=useState('');const [text,setText]=useState('');useEffect(()=>{api('/api/style').then(x=>{setProfile(x.profile||'');setText('')}).catch(e=>alert(e.message))},[]);async function save(){try{const x=await api('/api/style/learn',{method:'POST',body:JSON.stringify({text})});setProfile(JSON.stringify(x.profile,null,2));setText('')}catch(e:any){alert(e.message)}}return <Layout><h1>Writing Style</h1><p className="muted">Update the style MailPilot uses when drafting replies.</p><div className="card"><h2>Teach it with a fresh example</h2><textarea className="textarea" placeholder="Write a reply in your natural style…" value={text} onChange={e=>setText(e.target.value)}/><button className="btn primary" style={{marginTop:12}} onClick={save}>Relearn my style</button></div>{profile&&<div className="card" style={{marginTop:18}}><h2>Current style profile</h2><pre style={{whiteSpace:'pre-wrap'}}>{profile}</pre></div>}</Layout>}
